namespace UserWebservice.Configurations;

public static class TokenConstants
{
    // In order to avoid human error when typing the names, we define them as constants.
    public const string Issuer = "UserWebservice";
    public const string Audience = "UserWebservice";
}