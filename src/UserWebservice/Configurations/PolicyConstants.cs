namespace UserWebservice.Configurations;

public static class PolicyConstants
{
    // In order to avoid human error when typing the policy names, we define them as constants.
    public const string RateLimitPerIp = "PerIpRateLimit";
}